rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /bingo_grids/{document=**} {
      // 🟢 Publique : Tout le monde peut lire les grilles
      allow read: if true;
      
      // 🟢 Publique : Tout le monde peut créer, modifier (voter/cocher) et supprimer
      // (Nécessaire car tu as voulu permettre la suppression par tous)
      allow write: if true;
    }
  }
}
